/**
 * Audience.tsx
 *
 * Audience Insights page - displays follower analytics and growth metrics.
 * Uses standard glass-morphism aesthetic matching the rest of the application.
 */

import AudienceDashboard from '../components/audience/AudienceDashboard';

export default function Audience() {
  return <AudienceDashboard />;
}
